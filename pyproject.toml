[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/daksh"]

[project]
name = "daksh"
version = "0.1.0"
description = "highly skilled, competent, and precise AI agent"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
